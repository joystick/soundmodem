{
  "name": "soundmodem",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "rollup -c -w",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "rollup": "^4.0.0",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "vitest": "^3.0.0"
  }
}
